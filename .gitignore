# Local config (contains hosts / credentials)
endpoints.json
servers.json

# Runtime
*.log
.env
.DS_Store

# In case anyone runs npm here — flantastic needs no dependencies
node_modules/
package-lock.json
